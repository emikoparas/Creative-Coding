
function setup(){
  createCanvas(400,400);
}
function draw(){
  background(220);
  fill(0,0,0)
  textFont('Monoton');
  //'monoton' is the font name in the link.
  text('The Art of Noticing',125,200);
  textSize(24)
}